# Environment for the aux-35b service in docker-compose.example.yml.
# Copy to compose.env.aux and edit. NEVER commit the real file.

# vLLM reads bearer auth from this exact variable name
VLLM_API_KEY=change-me-aux
VLLM_SERVED_MODEL_NAME=aux-35b
